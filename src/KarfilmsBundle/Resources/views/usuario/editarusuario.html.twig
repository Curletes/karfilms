{% extends '@Karfilms/layout.html.twig' %}

{% block body %}
    <div class="col-lg-6">
        <h2>Editar perfil</h2>
        <hr>
        <img height="50" width="50" src="{{ asset('imagenes/perfiles/' ~ icono) }}">
        {{ form_start(form, {'action':'', 'method':'POST'}) }}
        {{ form_end(form) }}
    </div>
    <div class="clearfix"></div>
    <a href="{{ path("perfil_usuario", {"id":app.user.id}) }}" class="btn btn-secondary">Cancelar</a>
{% endblock %}